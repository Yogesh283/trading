# Accidental nested clone of this repo — never commit as submodule
trading/

node_modules
# TypeScript incremental build caches (regenerated by `tsc -b`; never commit)
*.tsbuildinfo

dist
logs
data/*.sqlite
data/*.db
.env
frontend/.env
coverage
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.DS_Store
# Large Android builds — deploy via server / releases/ folder instead
releases/*.apk
frontend/public/downloads/*.apk
# Vim / editors
*.swp
*~
