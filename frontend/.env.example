# Live / Production build ke liye: API base URL (trailing slash mat rakhein)
# Example: VITE_API_URL=https://api.yourdomain.com
# Build: npm run frontend:build — yeh value bundle mein embed ho jati hai.
# VITE_API_URL=
