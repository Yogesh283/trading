/** Level income: each upline level gets this fraction of bet stake (0.1% = 0.001). */
export const LEVEL_INCOME_FRACTION = 0.001;
/** Max upline levels paid when a live binary bet is placed. */
export const LEVEL_INCOME_DEPTH = 5;
